# Сервер: первый этап

Дата: 23.09.2026. Работа выполнена в клоне официального репозитория `BAITC-Hacks/hack-f0370cc6-rm-edtech`, ветка `feat/backend`, от `f19c8d4` (начальный main). Коммит `3e9ba86` импортирует без изменения предоставленные starter и frontend-stage-1. Серверный этап — отдельный последующий коммит; точный SHA: `git log -1 --format=%H`. Ветка пока локальная, push/PR/слияние в main не выполнялись.

## Реализовано

- `GET /api/state`: tasks с вычисленным rating, teams с вычисленными points, proposals, drafts, meta по общему контракту.
- `GET /api/catalog`: только published === true, rating по убыванию, publishedAt по возрастанию при равенстве. Параметры industry, level, query необязательны; пустые значения снимают фильтр. Слабые опубликованные задачи не скрываются.
- Раздача public/ с того же origin, правильные MIME для ES-модулей и CSS, HEAD для статики. При отсутствии index.html — технический текст, API остаётся доступным.
- JSON-хранилище по DATA_FILE, первый seed сохраняется на диск, записи последовательны через временный файл и rename. Повреждённая база не заменяется seed молча.
- Ошибки JSON `{error:{code,message,fields:{}}}`. Неверные/повторные параметры каталога — 400; неизвестный маршрут — 404; неверный HTTP-метод на реализованном GET — 405 с Allow. Внутренняя ошибка — 500 без стека в ответе.
- Выдача файлов ограничена public/, включая проверку реального пути и запрет dotfiles/Windows ADS.

## Исправления заготовок

- `cleanFields`: отклоняет null, массивы, скаляры, неизвестные ключи и значения не строкового типа. Пропущенные поля становятся пустыми строками; длины проверяются.
- Каталог строго различает опубликованную задачу и черновик. Query убирает крайние пробелы и ищет в тегах, как присланный preview frontend.
- `store.mutate()` возвращает отдельную копию результата, чтобы вызывающий код не мог менять сохранённое состояние без записи на диск.
- `src/state.mjs`: points не берутся из сохранённого team.points; рассчитываются по подтверждённым уникальным ключам этапов принятых предложений с подтверждением владельца задачи. Очки milestone берутся из STAGES, а не из произвольного persisted points. Это чтение существующих данных; HTTP-подтверждение этапов пока отсутствует.
- `src/seed.mjs` не менялся: все четыре набора содержат по пять примеров.

## Запуск

Node.js 20+; сторонние зависимости не нужны. Из корня репозитория:

```sh
node --version
npm test
npm start
```

Открыть `http://127.0.0.1:3000/` **без `?preview=1`**. По умолчанию сервер слушает только loopback; это локальное демо с синтетическими данными и без настоящей авторизации. PORT — целое 1–65535; DATA_FILE — путь к JSON, по умолчанию data/state.json в корне проекта. Относительный DATA_FILE разрешается относительно рабочей папки процесса. Один процесс сервера на один DATA_FILE.

В текущем окружении системный PATH выбирал Node 18.16.0 (ниже требования), а sandbox блокировал дочерние тестовые процессы. Успешный запуск и `npm test` выполнены с Node **24.19.0**, временно добавленным в PATH для команды. Системная установка не менялась. Не сообщать, что тестирование на Node 18 или отдельно на Node 20 выполнено.

## Проверка

`npm test`: **24/24** успешно — 19 новых backend/integration и 5 исходных тестов frontend transport.

Проверены: наборы/рейтинг; границы 39/40, 69/70, 89/90; 0 и 20 баллов у опубликованных задач; скрытый неопубликованный черновик; порядок при равных баллах; комбинированные фильтры и теги; только подтверждённые поля; удаление поля уменьшает рейтинг; строгие fields; производные очки; GET JSON/meta; HTTP-ошибки; статика и недоступность серверных файлов; отсутствие index; повторное открытие сервера с той же базой; чтение API неизменённым public/api.js; последовательные мутации; отказ записи без потери состояния; восстановление очереди; повреждённая база.

В браузере на живом сервере без preview проверены: индикатор «API подключён», пять карточек с рейтингами 100/90/65/45/20, открытие task_service с расшифровкой 20 баллов и недостающими полями, фильтр «Туризм» с одной карточкой 65 баллов. public/** побайтово совпадает с архивом frontend-stage-1.

## Контракт и расхождения

- Структуры реализованных GET соответствуют docs/API_CONTRACT.md; файл контракта не менялся.
- Поиск по тегам и trim выравнивают backend с preview. Текст контракта не перечисляет области поиска — первому Codex предлагается явно дописать их.
- В контракте не заданы 405 и конкретная политика неправильных query: описаны выше, предлагается внести это уточнение без изменения схем.
- meta.aiMode='local' означает выбранный режим конфигурации без внешнего API. **Это не доказательство работающей AI-функции**: POST /api/ai/analyze не реализован и возвращает 404. Не показывать сообщение «AI готов» по одному этому полю.
- /api/state по контракту включает неопубликованные задачи для демонстрационной роли; каталог должен использовать /api/catalog. Это не production-разграничение доступа.
- package.json и .gitignore не менялись. В package.json исходного starter нет preview script, хотя frontend docs его упоминают: добавить владельцу корня. До этого отдельный preview можно запустить `node scripts/preview.mjs`.
- Перед этапом секретов/публикацией добавить в .gitignore `.env.*`, исключение для безопасного `.env.example`, `data/*.json.tmp`; сам ignore не очищает историю Git.

## Сверка с оригинальным ТЗ

Получен Markdown «HackAlem AI_ кейс по геймификации практических заданий.md». Он подтверждает продукт, шкалу оценки, минимум данных, низкорейтинговую доступность и допустимость локальной AI-заглушки при показе промпта/формата/обработки ошибок. Сквозной сценарий, AI и запись через API ещё не реализованы — полный MVP не завершён.

Организационное расхождение: в кейсе команда 3–5 человек, в ранее прочитанном общем положении 1–3. Для вашей команды из двух требуется уточнение организаторов, а не наше предположение. ТЗ допускает архив как артефакт, но общее положение требует основной разработки и сдачи в выданном GitHub-репозитории: используем официальный repo и форму платформы.

## Следующий серверный этап

POST /api/tasks → POST /api/ai/analyze → PUT /api/tasks/:id с confirmed:true → POST /api/tasks/:id/publish. Включить промпт и примеры AI, минимум три уместных вопроса, валидацию результата, сохранение человеческих ответов, ошибки и 409 версии. Затем отдельным этапом отклики, ручные решения и подтверждённые этапы. В первом этапе мутации не добавлялись.
