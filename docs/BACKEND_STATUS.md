# Сервер: каталог и создание черновика

Дата: 23.09.2026. Работа выполнена в клоне официального репозитория `BAITC-Hacks/hack-f0370cc6-rm-edtech`, ветка `feat/backend`, от `f19c8d4` (начальный main). Коммит `3e9ba86` импортирует без изменения предоставленные starter и frontend-stage-1. Проверенный серверный этап: `9bcbddb`. Координация и передача результатов теперь ведутся через ветки общего GitHub-репозитория. PR и слияние в main не выполнялись.

## Текущая задача и координация

- **Текущая задача:** отдельный согласованный этап `POST /api/ai/analyze`: локальная заглушка с честной маркировкой, 3–6 вопросами, сохранением человеческих ответов и проверкой выходного формата. Платный/внешний AI не подключается. POST /api/tasks уже опубликован в `8456e2c`.
- **Последняя синхронизация:** прочитаны удалённые FRONTEND_STATUS, COORDINATION и API_CONTRACT на `748898e`. `origin/feat/frontend` объединён в свою ветку без конфликтов; POST /api/tasks сохранён; 29/29 тестов после merge. Общие scripts/team-sync, инструкции и корневые настройки владельца приняты. Перед AI выполнен `npm run team:sync -- backend`.
- **Занятые файлы сейчас:** `server.mjs`, новые `src/ai/**`, `tests/backend/ai.test.mjs`, `tests/backend/http.test.mjs`, `docs/BACKEND_STATUS.md`. `public/**` и общий контракт самостоятельно не меняю.
- **Готово для frontend:** `POST /api/tasks`, `GET /api/state`, `GET /api/catalog`, раздача неизменённого `public/`, постоянная JSON-база. Открывать `/` без `?preview=1`. Схема создания и ответа сохранена по контракту.
- **Вопросы/действия frontend:** GET-интеграция и редакционные уточнения приняты. Для POST после публикации использовать role=business, businessId=business_demo, raw и industry из meta.industries. Форма не должна объявлять карточку опубликованной: результат создания — неопубликованный черновик с нулевым рейтингом. Блокирующих вопросов по согласованной схеме нет.
- **Проверки текущего шага:** `npm test` — **29/29** на Node 24.19.0. Добавлено пять групп HTTP-проверок создания, валидации, параллельных запросов, сохранения после перезапуска и лимита тела.
- **Нужно от frontend:** подключить первый шаг конструктора к опубликованному endpoint, показать сохранённый черновик и не называть его опубликованной карточкой. Отметить результат в своём удалённом статусе. При следующем редакционном обновлении контракта указать Content-Type application/json и лимит тела 64 КиБ; формат запроса/ответа менять не нужно.
- **Следующий отдельный этап:** после AI — PUT /api/tasks/:id с ручным подтверждением, затем публикация; они пока не реализованы. До итогового сообщения этого этапа AI endpoint не считать готовым.

Правило дальнейшей работы: перед каждым небольшим этапом `git fetch origin`, затем чтение `docs/FRONTEND_STATUS.md` именно из `origin/feat/frontend`. Если ветки или файла нет, записать это здесь и продолжать независимую часть в согласованных границах. Перед изменениями отмечать текущую задачу и занятые файлы; после законченного шага фиксировать проверки, вопросы, ограничения, делать commit и обычный push в `feat/backend`. Чужие ветки не менять, force push не использовать. Для изменения контракта публиковать конкретное предложение здесь, читать ответ в удалённом FRONTEND_STATUS и только после согласования менять общую спецификацию через её владельца. Пересылка статусов через человека больше не требуется.

## Реализовано

- `POST /api/tasks`: принимает JSON `{role:"business",businessId:"business_demo",raw,industry}`, возвращает **201** `{task}`. Raw — обязательная строка до 4000 символов, industry из meta.industries. Неизвестные поля запроса отклоняются, серверные id/dates/version/fields/confirmation/publication формируются сервером.
- `GET /api/state`: tasks с вычисленным rating, teams с вычисленными points, proposals, drafts, meta по общему контракту.
- `GET /api/catalog`: только published === true, rating по убыванию, publishedAt по возрастанию при равенстве. Параметры industry, level, query необязательны; пустые значения снимают фильтр. Слабые опубликованные задачи не скрываются.
- Раздача public/ с того же origin, правильные MIME для ES-модулей и CSS, HEAD для статики. При отсутствии index.html — технический текст, API остаётся доступным.
- JSON-хранилище по DATA_FILE, первый seed сохраняется на диск, записи последовательны через временный файл и rename. Повреждённая база не заменяется seed молча.
- Ошибки JSON `{error:{code,message,fields:{}}}`. Неверные/повторные параметры каталога и неверное JSON-тело — 400; неверная демо-роль/владелец — 403; неизвестный маршрут — 404; неверный HTTP-метод реализованного endpoint — 405 с Allow; тело более 64 КиБ — 413. Внутренняя ошибка — 500 без стека в ответе.
- Выдача файлов ограничена public/, включая проверку реального пути и запрет dotfiles/Windows ADS.

## Исправления заготовок

- `cleanFields`: отклоняет null, массивы, скаляры, неизвестные ключи и значения не строкового типа. Пропущенные поля становятся пустыми строками; длины проверяются.
- Каталог строго различает опубликованную задачу и черновик. Query убирает крайние пробелы и ищет в тегах, как присланный preview frontend.
- `store.mutate()` возвращает отдельную копию результата, чтобы вызывающий код не мог менять сохранённое состояние без записи на диск.
- `src/state.mjs`: points не берутся из сохранённого team.points; рассчитываются по подтверждённым уникальным ключам этапов принятых предложений с подтверждением владельца задачи. Очки milestone берутся из STAGES, а не из произвольного persisted points. Это чтение существующих данных; HTTP-подтверждение этапов пока отсутствует.
- `src/seed.mjs` не менялся: все четыре набора содержат по пять примеров.

## Запуск

Node.js 20+; сторонние зависимости не нужны. Из корня репозитория:

```sh
node --version
npm test
npm start
```

Открыть `http://127.0.0.1:3000/` **без `?preview=1`**. По умолчанию сервер слушает только loopback; это локальное демо с синтетическими данными и без настоящей авторизации. PORT — целое 1–65535; DATA_FILE — путь к JSON, по умолчанию data/state.json в корне проекта. Относительный DATA_FILE разрешается относительно рабочей папки процесса. Один процесс сервера на один DATA_FILE.

В текущем окружении системный PATH выбирал Node 18.16.0 (ниже требования), а sandbox блокировал дочерние тестовые процессы. Успешный запуск и `npm test` выполнены с Node **24.19.0**, временно добавленным в PATH для команды. Системная установка не менялась. Не сообщать, что тестирование на Node 18 или отдельно на Node 20 выполнено.

## Проверка

`npm test`: **29/29** успешно — 24 backend/integration и 5 исходных тестов frontend transport. Тесты HTTP используют временную базу и не меняют демонстрационную базу сервера.

В POST-проверках подтверждены: 201 и все серверные поля; отсутствие черновика в каталоге; сохранение того же task после повторного открытия сервера; отказ чужой роли/ID; обязательность/длина/тип описания и industry; невозможность передать свои rating/published/confirmedFields/id; четыре параллельных записи без потерь; 400 для неверного JSON/Content-Type; 413 с Content-Length и chunked-передачей; 405 для GET /api/tasks. После ошибочных запросов число задач не меняется.

Проверены: наборы/рейтинг; границы 39/40, 69/70, 89/90; 0 и 20 баллов у опубликованных задач; скрытый неопубликованный черновик; порядок при равных баллах; комбинированные фильтры и теги; только подтверждённые поля; удаление поля уменьшает рейтинг; строгие fields; производные очки; GET JSON/meta; HTTP-ошибки; статика и недоступность серверных файлов; отсутствие index; повторное открытие сервера с той же базой; чтение API неизменённым public/api.js; последовательные мутации; отказ записи без потери состояния; восстановление очереди; повреждённая база.

В браузере на живом сервере без preview проверены: индикатор «API подключён», пять карточек с рейтингами 100/90/65/45/20, открытие task_service с расшифровкой 20 баллов и недостающими полями, фильтр «Туризм» с одной карточкой 65 баллов. public/** побайтово совпадает с архивом frontend-stage-1.

## Контракт и расхождения

- Структуры GET и POST /api/tasks соответствуют docs/API_CONTRACT.md; backend файл контракта не менял. Редакционные уточнения GET приняты владельцем в origin/feat/frontend на fbb3981.
- Поиск по тегам и trim выравнивают backend с preview. Текст контракта не перечисляет области поиска — первому Codex предлагается явно дописать их.
- В контракте не заданы 405 и конкретная политика неправильных query: описаны выше, предлагается внести это уточнение без изменения схем.
- meta.aiMode='local' означает выбранный режим конфигурации без внешнего API. **Это не доказательство работающей AI-функции**: POST /api/ai/analyze не реализован и возвращает 404. Не показывать сообщение «AI готов» по одному этому полю.
- /api/state по контракту включает неопубликованные задачи для демонстрационной роли; каталог должен использовать /api/catalog. Это не production-разграничение доступа.
- package.json и .gitignore не менялись. В package.json исходного starter нет preview script, хотя frontend docs его упоминают: добавить владельцу корня. До этого отдельный preview можно запустить `node scripts/preview.mjs`.
- Перед этапом секретов/публикацией добавить в .gitignore `.env.*`, исключение для безопасного `.env.example`, `data/*.json.tmp`; сам ignore не очищает историю Git.

## Сверка с оригинальным ТЗ

Получен Markdown «HackAlem AI_ кейс по геймификации практических заданий.md». Он подтверждает продукт, шкалу оценки, минимум данных, низкорейтинговую доступность и допустимость локальной AI-заглушки при показе промпта/формата/обработки ошибок. Создание черновика через API реализовано. Сквозной сценарий, AI, подтверждение, публикация и отклики ещё не реализованы — полный MVP не завершён.

Организационное расхождение: в кейсе команда 3–5 человек, в ранее прочитанном общем положении 1–3. Для вашей команды из двух требуется уточнение организаторов, а не наше предположение. ТЗ допускает архив как артефакт, но общее положение требует основной разработки и сдачи в выданном GitHub-репозитории: используем официальный repo и форму платформы.

## Следующий серверный этап

POST /api/tasks завершён. Следом отдельными шагами: POST /api/ai/analyze → PUT /api/tasks/:id с confirmed:true → POST /api/tasks/:id/publish. Включить промпт и примеры AI, минимум три уместных вопроса, валидацию результата, сохранение человеческих ответов, ошибки и 409 версии. Затем отклики, ручные решения и подтверждённые этапы.

## Запрос для первого шага конструктора

```http
POST /api/tasks
Content-Type: application/json

{"role":"business","businessId":"business_demo","raw":"Заявки клиентов теряются в чатах","industry":"Услуги"}
```

Ответ 201: `{task}` с id, raw, industry, company="Мой бизнес", пустыми fields, confirmedFields=[], confirmedAt=null, published=false, publishedAt=null, version=1, серверными createdAt/updatedAt и rating.score=0. После успешного ответа использовать возвращённый task, не рассчитывать рейтинг на клиенте. Это демо-идентификация, а не аутентификация. Повторная отправка POST создаёт новый черновик: UI должен блокировать кнопку на время запроса и не повторять POST автоматически при неопределённом результате сети.
