# Frontend: TASKER — витрина компонентов

## Текущий этап завершён: Commissioner и подсветка подтверждённых условий

Сейчас делаю: визуальный этап завершён, результат публикуется в feat/frontend. Пользователь подтвердил все три пункта сразу: Commissioner, строгую сетку и световые окна подтверждённых полей, отсутствие иллюстраций. Дополнительное согласование этих решений не требуется.
Изменённые файлы: public/tasker-system.html; public/tasker/system.css, system.js, tokens.css; public/tasker/fonts/Commissioner-Variable.woff2, OFL.txt, README.md; docs/BRAND_DIRECTION.md, docs/FRONTEND_README.md, docs/FRONTEND_STATUS.md. Основное приложение, транспорт, API, сервер и seed не менялись.
Готово: локальный Commissioner, заголовки 650 и строгая сетка; все девять цветов сохранены. В строке каталога — краткие условия и компактная шкала. В раскрытом паспорте — семь подписанных окон со взвешенными частями полей; пропуски сохраняют своё место. Подтверждённая строка освещена primary-light с индиговой кромкой. Отдельные состояния для отсутствующих и неподтверждённых сведений. Нажатие на окно переводит фокус к разделу. Шкала и паспорт читают серверный снимок, итоговый рейтинг и баллы групп не пересчитываются.
Также работают: выбор из пяти опубликованных примеров, поиск, совместные фильтры и сброс пустой выдачи. Пустая выдача очищает паспорт и отключает диалог; прежний пример не выдаётся за текущий. Сохранение и навигация в витрине остаются честно обозначенными образцами без записи.
Проверки: npm test 32/32 на Node 20.9.0, node --check и git diff --check. Первый запуск HTTP-тестов был заблокирован песочницей (listen EPERM); запуск с разрешённым локальным listen прошёл полностью, с временными базами. Проверено буквальное совпадение девяти цветовых токенов с предыдущей утверждённой витриной и отсутствие новых hex.
Браузер: для рейтингов 20/45/65/90/100 подтверждено 2/4/5/7/10 полей соответственно; семь окон в каждом примере. У задачи на 45 баллов «Данные» не освещены, а следующие «Результат» и «Пользователи» освещены. Проверены переход и фокус на группе данных, пустой поиск и восстановление выдачи. На 1440 px проверена пропорция окон; на 664/390/320 px при UI 16 px нет горизонтального переполнения страницы, полей или управления, высота кнопок окон 44 px. Диалог без переполнения на телефоне, Tab/Shift+Tab по кругу, Escape и возврат фокуса. Консоль без ошибок и предупреждений.
Просмотр: http://127.0.0.1:3000/tasker-system.html. Для финальной проверки запущен сервер с DATA_FILE=/private/tmp/hackalem-tasker-review-20260923.json; рабочая база не использовалась для тестовых записей. Витрина не отправляет мутаций.
Синхронизация: npm run team:sync -- frontend выполнен, прочитан origin/feat/backend e1e35c6. Прочитан статус готового v1 (84 теста) и отдельного предложения PLATFORM_V2_PROPOSAL; новое API не согласовываю заочно в визуальном этапе. Серверная ветка не объединяется; локальная сборка остаётся прежней, 32 теста.
Нужно от коллеги: public/** не менять. Для этого этапа серверных изменений не требуется. Предложение v2 рассматривается отдельным этапом.
Следующий шаг: перенос утверждённой системы в основной интерфейс — отдельный небольшой этап. Этот результат ограничен витриной каталога и паспорта; редактор с PUT и автоматическим обновлением после подтверждения здесь не подключён. Фоновое чтение GitHub после завершения не ведётся.

## Предыдущая витрина: Georgia Bold и Verdana

Сейчас делаю: этап завершён, остановка до явного «ок» пользователя. Витрина: http://127.0.0.1:3000/tasker-system.html. Основное приложение ещё не перевёрстано.
Изменённые файлы: public/tasker-system.html; public/tasker/tokens.css, system.css, system.js, mark.svg; docs/FRONTEND_STATUS.md, docs/BRAND_DIRECTION.md, docs/FRONTEND_README.md. Исходные public/app.js, styles.css, index.html, api.js и favicon.svg сохранены.
Готово: все девять цветов совпадают с утверждёнными значениями; Georgia Bold + Verdana; знак и TASKER; навигация без счётчиков; кнопки, поле, строка существующей демо-задачи, четыре статуса, семь сегментов рейтинга и работающий диалог. Витрина читает существующий API и не отправляет записи. Навигация и кнопки сохранения демонстрируют состояния, не объявляют выполнение операций.
Проверки этого этапа: npm test 32/32 на Node 20.9.0, 0 ошибок (повтор после финальных правок). Первому запуску песочница запретила localhost listen; вне этого ограничения весь набор прошёл. node --check прошёл. В браузере 9 сочетаний ширин 1440/664/390 и Verdana 14/15/16 px: горизонтального переполнения страницы и контролов нет, обе кнопки образцов 44 px по высоте. На узких экранах переносятся целые фильтры, подписи не обрезаются. Оставлена Verdana 14 px для компактного UI и 16 px в поле; альтернативная гарнитура не понадобилась.
Браузерные проверки: новая навигация без счётчиков, фильтры и пустое сочетание, ввод, честный отклик демонстрационных кнопок, рейтинг 20 и 90 из API. Диалог: открытие, Tab/Shift+Tab по кругу, Escape и возврат фокуса. Убрано разбиение «/ 100» на телефоне. Консоль без предупреждений/ошибок. Числа рейтинга не пересчитываются. Основные пары контраста: text/surface 15.10:1, text/bg 14.10:1, primary/bg 5.84:1, muted/surface 4.52:1, surface/primary 6.25:1; девять hex не менялись.
Синхронизация: прочитан origin/feat/backend до 29077ff; серверные изменения не объединялись. Новые продуктовые предложения прочитаны как статус коллеги, не включены в согласованный визуальный этап.
Нужно от коллеги: public/** не менять; продолжать серверную работу в своей ветке. Контракт, формула, данные и сервер остаются прежними.
Следующий шаг: только после явного «ок» пользователя применить систему к каталогу и паспорту. После них снова остановиться.

## Ранее интегрированная сборка

Текущий этап: завершена первая версия «alem / бюро практики» и форма исходной потребности.
Сейчас делаю: этап закончен; результат публикуется в feat/frontend и PR #1. Общая серверная база получена до 8de4f2d, включая POST /api/tasks. Следующий этап не начат.
Занятые файлы: незавершённой работы нет. Область frontend остаётся public/**, tests/e2e/**, корневые настройки и контракт.
Готово: каталог, поиск/фильтры, два вида выдачи, избранное, команды, паспорт задачи с серверной расшифровкой рейтинга, форма создания и повторное открытие сохранённых черновиков.
Проверки: 32/32 теста на Node 20.9.0; браузер с отдельной тестовой базой — валидация пробельного ввода, POST, нулевой рейтинг, отсутствие нового черновика в каталоге, чтение после перезагрузки, поиск, фильтр 0–39, избранное, Escape, пять команд. Ширины 1440, 664 и 390: горизонтального переполнения нет, мобильная кнопка создания доступна.
Изменения контракта: схемы не менялись; в начале API_CONTRACT отмечена готовность POST /api/tasks.
Ответ коллеге: POST /api/tasks принят и подключён. Результат создания хранится как неопубликованный task; повторный POST автоматически не выполняется. При потере подтверждения UI предлагает прочитать существующие черновики. Preview не выполняет записей.
Последняя синхронизация: прочитан backend b6bac39 и PRODUCT_QUALITY. В этой сборке пока интегрирован сервер до 8de4f2d; опубликованный AI 91bc128 примем отдельным следующим шагом. Семантика AI согласована: непустой answer заменяет поле, пустой не стирает; отсутствующий context получает raw дословно, явно пустой остаётся пустым. На первом анализе передадим fields={}, после редактирования — полный текущий снимок. Это согласование контракта, не заявление о проверке AI-интеграции.
Продуктовые предложения коллеге: материалы/результат/приёмка уже выделены в каталоге и паспорте. После PUT покажем разницу между двумя серверными rating.breakdown; до сохранения улучшение не объявляем. API расширять не требуется.
Нужно от коллеги: продолжать подтверждение/публикацию по согласованному контракту и публиковать готовность в BACKEND_STATUS. Айдентика не требует изменения API, формулы рейтинга или seed. public/** не менять.
Следующий шаг frontend: интеграция уточняющих вопросов, редактирования, подтверждения и публикации после проверки соответствующих endpoints. Работать отдельными небольшими этапами.

## Что изменилось в продукте

- Имя alem сохранено. Позиционирование — бюро практики, тезис «Знания — в дело».
- Убран боковой SaaS-шаблон; введены верхняя навигация, спокойная типографика, тёплая бумага и зелёный акцент.
- Собственный знак открытой рамки, favicon, оформление карточек как рабочих документов.
- Паспорт показывает подтверждённые сведения и незаполненные условия. В каталоге отдельно видны материалы, результат и приёмка.
- Семь сегментов шкалы соответствуют серверным группам rating.breakdown; ширина — max, заполнение — earned/max. Итоговый рейтинг клиент не вычисляет.
- Нет градиентных промоблоков, фальшивых пользовательских метрик, AI-звёздочек и лозунгов о «пространстве возможностей».
- Источники, принципы, ограничения и сценарий защиты: docs/BRAND_DIRECTION.md.

## Форма и данные

«Предложить задачу» открывает демонстрационную роль бизнеса. Ввод: raw до 4000 символов, industry из meta.industries. Отправляются только role=business, businessId=business_demo, raw, industry. Во время запроса поля и повторная отправка блокируются.

Успех показывается только после проверенного ответа 201. Черновик можно увидеть снова в разделе «Ваши черновики» на экране создания. Исходный текст без подтверждённых условий не получает баллы. Потеря сети/некорректное подтверждение не считаются успешной записью и не вызывают автоматический повтор POST.

Обычный запуск: npm start, http://127.0.0.1:3000/. Статический просмотр: npm run preview, http://127.0.0.1:4173/?preview=1. Preview явно обозначен и не подменяет неработающий API. В демо-среде примеры синтетические, роли не являются аутентификацией.

## Граница готовности

AI-вопросы, полноценное редактирование полей, ручное подтверждение, публикация, отправка откликов и решения бизнеса в интерфейс ещё не подключены. Просмотр seed-откликов не является реализацией отправки отклика. Полный сквозной MVP ещё не завершён.

Браузерные записи проверялись через отдельный DATA_FILE в /private/tmp; рабочая демонстрационная база тестами не изменялась. Серверные тесты также используют временные базы.
